/*
 * FHEMVIZ - Dimmer-Widget (pct/dim).
 *
 * GRUNDGERUEST / SCAFFOLD (v0.1.0) - Stub, kein Rendering.
 * Kern-Widget Nr. 3 (§9). Stellt einen Prozentwert dar und sendet bei
 * Interaktion "set <dev> pct <n>".
 */

import { FhemvizWidget } from "./base-widget.js";

export class FhemvizDimmer extends FhemvizWidget {
  update(/* device */) {
    // TODO (Bau-Session): Prozentwert/Slider darstellen.
  }
}
